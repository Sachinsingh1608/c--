#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    bool y;
    for(int i=2;i<=n;i++)
    {
        if(n%i===0)
        {
            y=true;
        }
        else 
        y=false;
    }
    if(y)
    cout<<"prime";
    else
    cout<<"not prime";
}